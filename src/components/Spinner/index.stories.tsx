import React from 'react';
import { Spinner } from './';

export default { title: 'component/Spinner', component: Spinner };

export const Default = () => <Spinner />;

Default.storyName = 'Default chart';
