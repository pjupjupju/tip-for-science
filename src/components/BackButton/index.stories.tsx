import React from 'react';
import { BackButton } from './';

export default {
  title: 'components/BackButton',
  component: BackButton,
};

export const Default = () => <BackButton>back</BackButton>;
Default.storyName = 'Default button state';
